# SQL Exercises 1
SQL Fundamentals: SELECT & Filtering
This Exercise we dive into SQL(STRUCTURED, QUERY, LANGUAGE) where we read the table, write the query that returns the requested data, and draw the expected output table.
The SQL functions we have learnt are: 
- SELECT
- FROM
- WHERE
- ODER BY
- LIMIT.
